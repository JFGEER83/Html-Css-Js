/* Types primitifs :
const name = "John"  string (toujour entre "")
const age = 31 (number)
const note = 4.5 (number)
const isEmpty = true ou false (boolean)
const x = null (var sans valeur, affiche object en console.log -> bug js)
const y = undefined (var indéfinie, normalement comme une const à tjr une valeur assignée,
                    il est rare de lui assigner un undefined)
let z (comme on est poas obligé d'assigner une valeur pour une var let, celle-ci est automatiquement undefined)
*/